# Analysis of selected model

Select proper model name (from the `./models` directory) in `model_analysis.py` and run the program. You will see the model structure, it's accuracy and confusion matrix.